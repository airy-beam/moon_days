Калькулятор лунных дней (Полнолуния и Новолуния)

# 📌 Назначение проекта

Этот проект предназначен для точного расчёта дат полнолуний и новолуний (включая случаи kṣaya tithi — «выпавшие» лунные дни) для разных городов и часовых поясов.

Результат генерации:
- C-заголовочный файл (.h) для использования в микроконтроллерах / embedded-проектах. Пример Arduino проекта в папке /arduino_sketch.
- CSV-файл для анализа и проверки

# 🔭 Методика расчёта

Расчёты выполняются с использованием библиотеки Swiss Ephemeris (Швейцарские эфемериды) — одного из самых точных астрономических инструментов.

Используемый принцип:
Вычисляются точные положения Солнца и Луны в эклиптических координатах.
Рассчитывается разность их долгот.

Лунное событие определяется по значению:
0° — новолуние (Amavasya)
180° — полнолуние (Purnima)

Событие привязывается к локальному дню через момент местного восхода Солнца.

Если между двумя восходами Солнца «перепрыгивается» нужная титхи — фиксируется Kṣaya-событие.

Точность:
- Основана на JPL/NASA эфемеридах
- Погрешность — доли секунды
- Используется в астрономии, навигации и профессиональной астрологии

🔗 Официальный сайт Swiss Ephemeris:
https://www.astro.com/swisseph/

# 🌍 Поддерживаемые города

Генерация выполняется для следующих городов:
- Mysuru
- Moscow
- Saint-Petersburg
- Yekaterinburg
- Krasnaya Polyana
- Novosibirsk
- Minsk
- Kyiv
- Astana
- Almaty

Для каждого города учитываются:
- Географические координаты
- Часовой пояс
- Локальный восход Солнца

# 📂 Генерируемые файлы
1️⃣ moon_events.h

Используется в embedded-проектах (Arduino и др.).

Содержит:

enum MoonEventKind {
  PURNIMA = 0,
  AMAVASYA = 1,
  KSHAYA_PURNIMA = 2,
  KSHAYA_AMAVASYA = 3,
};

struct MasterEvent { int index; uint16_t y, m, d; uint8_t kind; };
struct LocalEvent  { int index; int8_t delta; uint8_t kind; };

MASTER_EVENTS — эталон (Mysuru)

LOCAL_EVENTS — лунные дни для другого города (local).

2️⃣ CSV-файл

Формат таблицы:
- Тип события для Mysuru (PURNIMA / AMAVASYA, ※ если Kṣaya)
- Дата события в формате DD Mon YYYY
Далее — колонки для каждого города. Правила заполнения:
- Если дата совпадает с Mysuru → ячейка пустая
- Если дата отличается → показывается дата
- Если локально Kṣaya → ※
- Если дата отличается и Kṣaya → ※ (без даты)

# ⚙️ Основные функции

generate_moon_days(city)
Генерирует список событий для одного города

write_h(master, local)
Генерирует .h файл

generate_csv()
Генерирует CSV для всех городов и выводит количество событий для отладки

# ✅ Гарантии корректности

Количество событий одинаково для всех городов
Индексы массивов событий полностью синхронизированы
Исключены дубли и невозможные интервалы
Все Kṣaya-события корректно учитываются

# 🛠 Зависимости

- Python 3.9+
- pyswisseph
- pytz

Установка:
pip install pyswisseph pytz

# ⚠️ Примечание

Файлы генерируются автоматически.
Не редактируйте .h и .csv вручную — все изменения должны вноситься в генератор.
